services:
  - type: web
    name: motion-studio
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 2 main:app --bind 0.0.0.0:$PORT --timeout 120
    envVars:
      - key: RENDER
        value: "true"
      - key: DATABASE_URL
        value: ""
