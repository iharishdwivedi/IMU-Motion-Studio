services:
  - type: web
    name: motion-studio
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --worker-class geventwebsocket.gunicorn.workers.GeventWebSocketWorker -w 1 main:app --bind 0.0.0.0:$PORT --timeout 120
    envVars:
      - key: RENDER
        value: "true"
      - key: DATABASE_URL
        value: ""
